The package.json file was created with command:
$ npm init

The subsequent editing of package.json and addition of node modules was done with:
$ npm install <feature> --save

The typings/ folder was created and populated with TypeScript Definition Manager (TSD):
$ npm install -g tsd
$ tsd query node --action install
$ tsd query express --action install
